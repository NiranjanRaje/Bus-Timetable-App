# Safe heap profile for this constrained environment.
# This keeps Gradle alive while release builds finish without the daemon being killed.
org.gradle.jvmargs=-Xmx1536m -Xms256m -XX:MaxMetaspaceSize=768m -XX:ReservedCodeCacheSize=128m -Dfile.encoding=UTF-8
org.gradle.daemon=false
org.gradle.parallel=false
org.gradle.workers.max=1
android.useAndroidX=true
android.enableJetifier=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
